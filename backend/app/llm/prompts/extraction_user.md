Paper: $title
Passages $window_label. Each passage starts with a header [chunk_id=... | section=... | pages=...].

$passages

Extract the knowledge stated in these passages as one JSON object in the required format. Quote verbatim and cite the chunk_id of each quote.
