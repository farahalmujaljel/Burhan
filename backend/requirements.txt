# API
fastapi
uvicorn[standard]
python-multipart

# Data validation
pydantic
pydantic-settings

# PDF processing
PyMuPDF

# LLM
groq

# Knowledge layer (graph + vector evidence index)
neo4j
qdrant-client

# Local embeddings (numpy-only; works on Intel Macs). Optional alternative: fastembed
model2vec

# Environment variables
python-dotenv

# Utilities
httpx
rapidfuzz

# Testing
pytest

# Linting
ruff

# Added in later phases (kept out until needed):
#   langgraph            - Phase 6 agent orchestration
